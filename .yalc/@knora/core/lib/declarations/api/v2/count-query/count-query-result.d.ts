/**
 * Represents the result of a count query.
 */
export declare class CountQueryResult {
    readonly numberOfResults: number;
    /**
     *
     * @param numberOfResults total number of results for a query.
     */
    constructor(numberOfResults: number);
}
